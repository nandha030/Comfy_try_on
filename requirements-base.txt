# Comify Virtual Try-On - Base Requirements
# These dependencies work across all platforms (Windows/macOS/Linux)
# For platform-specific dependencies, see requirements-{platform}.txt

# ============== Web Framework ==============
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.6
aiofiles>=23.2.0
httpx>=0.25.0
websockets>=12.0
pydantic>=2.5.0
aiohttp>=3.9.0

# ============== Image Processing ==============
Pillow>=10.0.0
numpy>=1.24.0

# ============== AI/ML Core (platform-agnostic) ==============
# Note: PyTorch must be installed separately with platform-specific commands
# See requirements-{platform}.txt for PyTorch installation

diffusers>=0.25.0
transformers>=4.36.0
accelerate>=0.25.0
safetensors>=0.4.0

# ============== Segmentation & Processing ==============
segment-anything>=1.0
rembg>=2.0.50

# ============== Database ==============
aiosqlite>=0.19.0

# ============== Utilities ==============
tqdm>=4.66.0
requests>=2.31.0
huggingface-hub>=0.19.0
python-dotenv>=1.0.0

# ============== RunPod (for cloud deployment) ==============
runpod>=1.3.0
